\documentclass[preview,crop]{standalone}
\usepackage[utf8]{inputenc}

\usepackage{modml}
\usetikzlibrary{matrix}
\begin{document}
\inference
          {l \in \text{dom}(S)}
          {\compile{S}{S}\#m(l \bullet get) \longrightarrow^{*} \compile{S(l)}{v})}
\end{document}
